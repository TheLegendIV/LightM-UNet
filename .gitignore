dataset-prep/ARCADE/*
__pycache__/

# -- Heavy nnU-Net data/output dirs -------------------------------------------
# Preprocessed caches and training outputs are regenerated by the job scripts
# (nnUNetv2_plan_and_preprocess / nnUNetv2_train) -- never worth committing,
# for any dataset, including Dataset506/507.
data/nnUNET_preprocessed/*
data/nnUNET_results/*
data/nnUNET_results_pareto/*
data/nnUNET_results_smoke/*
data/nnUNET_results_smoke_test/*
data/*.log

*/imagesTs
*/labelsTs

# Raw datasets: keep only Dataset506/507 (current active work). Everything
# else (501/502/503/504/505, Dataset801/802, ...) is untracked -- regenerate
# via dataset-prep/ scripts if needed again. 504/505 were kept for a while
# during their active experiments but are untracked now too; still present
# on disk, just not in the repo going forward.
data/nnUNet_raw/*
!data/nnUNet_raw/Dataset506_stn_stx_oversampled/
!data/nnUNet_raw/Dataset507_ARCADE_refinement/
