# The winning config from section 2 (pruning), sourced by
# pruning_2b_fi_tune_array.job (uses everything except ENET_CHANNELS' f_i,
# which it sweeps) and postpruning_3a_quant_array.job (uses everything, and
# sweeps ENET_QUANT_BITS instead) -- edited in one place as section 2
# resolves, not duplicated per job script.
#
# PLACEHOLDER DEFAULTS below = O4 x native-bottleneck, this session's
# hardware-savings-midpoint reference pick DERIVED FROM ENET ORIGINAL
# (16,64,128,64,16), not E1 -- real test-set results showed Original beats
# E1 on Dice (0.780 vs 0.775) while already being smaller (369k vs 466k
# params), so the whole filter axis got re-derived from Original's
# proportions rather than E1's (see foundation_log.md's "Plan revision 2"
# section). O4 = (16,16,32,16,4) is O4's position in that new axis
# (O1=Original, O2, O4, O8, O16, OF), picked by the same methodology that
# picked U4 before: closest geometric-axis point to the O2-OF savings
# sub-range midpoint (5.4pts off, vs O8's 11.2pts off).
#
# NOT yet the real winner of section 2a's grid -- UPDATE
# ENET_CHANNELS/ENET_BOTTLENECKS after 2a
# (compression/slurm/pruning_2a_grid_array.job, 19 runs) completes and a
# human has picked the best cell meeting the Stage-1 goals. Update
# ENET_CHANNELS' f_i specifically after 2b
# (compression/slurm/pruning_2b_fi_tune_array.job, 3 runs, f_i in
# [12,8,4], reusing f_i=16 from 2a) picks the final f_i.
# ENET_DECODER_TYPE/USE_DILATED/USE_ASYMMETRIC/USE_STRIDED/USE_DSC come from
# cfinal_ops.env (1b/1c's output), not duplicated here -- both files get
# sourced together by 2b/3a.

export ENET_CHANNELS="16,16,32,16,4"
export ENET_BOTTLENECKS="4,8,8,2,1"
